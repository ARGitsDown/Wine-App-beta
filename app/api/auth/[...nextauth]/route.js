// Auth.js's own endpoints - the OAuth redirect, the callback, sign-out,
// the session lookup. Every one of them is generated; this file only
// exposes them, which is the shape v5 documents.
export { GET, POST } from "@/lib/auth";
